1. useful shell script snipped.
2. reusable code from work
3. projects which could be a build block of more complicated projects.

TODO:
1.scripts using ffmpeg, convert any audio to mp3, ogg, you can choose  bitrate, and some
other important properties of audio file.

2. script using ffmpeg get an audio file from video file

3. script using ffmpeg, convert video file to defined size ,format, any other important properties.

4. for a directory, rebuild an exactly same directory hierarchy, convert all audio file in  it to format.

This is a test for cmder git.   [to test if commit is valid]
